# mermaid‑parcel‑demo  
*A minimal demo of using Mermaid diagrams with Parcel.*

---

## 1. Overview

`mermaid-parcel-demo` is a tiny, self‑contained example that shows how to:

* Bundle Mermaid with Parcel
* Render Mermaid diagrams in the browser
* Build a production‑ready distribution

The demo is intentionally lightweight – it contains only the files you need to get started.

---

## 2. Repository Structure

```
mermaid-parcel-demo/
├─ .editorconfig
├─ .gitignore
├─ README.md
├─ index.html
├─ index.js
├─ package.json
├─ yarn.lock
└─ dist/          ← generated by `yarn release`
```

| File | Purpose |
|------|--------|
| `index.html` | The HTML page that loads the bundled JavaScript and contains a Mermaid diagram. |
| `index.js`  | Imports Mermaid, configures it, and renders the diagram. |
| `package.json` | Project metadata, scripts, and dependencies. |
| `dist/` | Production‑ready files created by `yarn release`. |

---

## 3. Getting Started

### 3.1 Prerequisites

* Node.js (≥ 12)  
* Yarn (or npm)

### 3.2 Install Dependencies

```bash
# Clone the repo
git clone https://github.com/mermaidjs/mermaid-parcel-demo.git
cd mermaid-parcel-demo

# Install deps
yarn
```

### 3.3 Run the Development Server

```bash
yarn server
```

Open <http://localhost:1234> in your browser.  
You should see a rendered Mermaid diagram.

---

## 4. Demo Files

### 4.1 `index.html`

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>Mermaid + Parcel Demo</title>
</head>
<body>
  <!-- Mermaid diagram -->
  <div class="mermaid">
    graph TD;
      A[Start] --> B{Decision};
      B -->|Yes| C[Process];
      B -->|No| D[End];
  </div>

  <!-- Bundle entry point -->
  <script src="./index.js"></script>
</body>
</html>
```

### 4.2 `index.js`

```js
import mermaid from 'mermaid';

mermaid.initialize({
  startOnLoad: true,
  theme: 'default',
  // Any other Mermaid configuration goes here
});
```

> **Tip** – If you want to customize the diagram’s appearance, edit the `mermaid.initialize` options.

---

## 5. Building for Production

```bash
yarn release
```

Parcel will bundle the code, minify it, and place the output in the `dist/` directory.  
Open `dist/index.html` to view the production build.

---

## 6. Scripts

| Script | Description |
|-------|------------|
| `yarn server` | Starts Parcel’s dev server (port 1234). |
| `yarn release` | Builds a production‑ready bundle in `dist/`. |

---

## 7. Extending the Demo

* **Add more diagrams** – Place additional `<div class="mermaid">...</div>` blocks anywhere in `index.html`.  
* **Use custom themes** – Install a Mermaid theme package and import it in `index.js`.  
* **Integrate with frameworks** – Replace `index.js` with a framework entry point (React, Vue, etc.) and import Mermaid as needed.

---

## 8. License

MIT © 2017–2025 MermaidJS

---

### 8.1 FAQ

**Q:** *Why Parcel?*  
A: Parcel is zero‑config, fast, and bundles everything automatically – perfect for demos.

**Q:** *Can I use this in a larger project?*  
A: Absolutely. Copy the `index.js` logic into your own build pipeline.

---

Happy diagramming! 🚀